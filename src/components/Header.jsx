const Header = ({ title }) => {
    return(
        <header className="bg-black text-light px-3 py-4 mb-5">
            <h1 className="h2 fw-normal">{title}</h1>
        </header>
    )
}

export default Header;